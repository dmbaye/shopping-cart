<table class="table">
    <tr>
        <td>Sub Total</td>
        <td>${{ basket.subTotal()|number_format(2) }}</td>
    </tr>
    <tr>
        <td>Shipping Rate</td>
        <td>$5</td>
    </tr>
    <tr>
        <td class="bg-success text-white">Total</td>
        <td class="bg-success text-white">${{ (basket.subTotal() + 5)|number_format(2) }}</td>
    </tr>
</table>
